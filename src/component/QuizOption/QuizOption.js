import React from 'react';

const QuizOption = ({options}) => {
    console.log(options)
    return (
        <div className=''>
            {options}
        </div>
    );
};

export default QuizOption;